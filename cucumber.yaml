server_config: -p prd -p chrome -p headless --tags @server_config
default: --format pretty -p prd -p html --tags @regression
default_scenario:  --format pretty -p prd -p html

# Reporters
html: --format html --out=reports/report.html

# Environments
hml: ENVIRONMENT='hml'
prd: ENVIRONMENT='prd'

#Browsers
chrome: BROWSER='my_chrome'
edge: BROWSER='my_edge'
firefox: BROWSER='my_firefox'

# Visual Model
headless: HEADLESS='true'
